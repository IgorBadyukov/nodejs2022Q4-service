import { IAlbum, IArtist, IFavorites, ITrack } from '../utils/types/types';

export const favourites = {
  artists: [] as IArtist[],
  albums: [] as IAlbum[],
  tracks: [] as ITrack[],
} as IFavorites;
